SortableSettings = {
   group       : "",  // or { name: "...", pull: [true, false, clone], put: [true, false, array] }
   sort        : true,  // sorting inside list
   // delay       : 0, // time in milliseconds to define when the sorting should start
   // disabled    : false, // Disables the sortable if set to true.
   // store       : null,  // @see Store
   // animation   : 150,  // ms, animation speed moving items when sorting, `0` — without animation
   // handle      : "",  // Drag handle selector within list items
   // filter      : "",  // Selectors that do not lead to dragging (String or Function)
   // draggable   : "",  // Specifies which items inside the element should be sortable
   // ghostClass  : "",  // Class name for the drop placeholder
   // chosenClass : "",  // Class name for the chosen item
   // dataIdAttr  : "",
   // forceFallback: false,  // ignore the HTML5 DnD behaviour and force the fallback to kick in
   // fallbackClass: "",  // Class name for the cloned DOM Element when using forceFallback
   // fallbackOnBody: false ,
   onEnd: null
}
